# Tailscale Split DNS Environment Variables

# Tailscale IP address of your VPS
# This is where the DNS server will listen
TAILSCALE_IP=100.116.229.118

# Domain to handle
DOMAIN=erodev.cloud

# Upstream DNS servers (used for non-matching queries)
UPSTREAM_DNS_1=1.1.1.1
UPSTREAM_DNS_2=1.0.0.1
